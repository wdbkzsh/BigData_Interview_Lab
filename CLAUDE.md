# BigData Interview Lab

## 项目目标

这是一个面向大数据开发校招的个人学习与复习平台。

核心目标：

学习 → 做题 → 错题 → 复习 → 掌握度统计。

## 第一阶段功能

1. 每日学习任务
2. 选择题（自动判分）
3. 问答题（用户自评掌握状态）
4. SQL 手写题（AI 辅助判题，不依赖真实数据库执行）
5. 错题本
6. 间隔复习（基于艾宾浩斯遗忘曲线算法）
7. 学习统计

## 技术栈

前端：
- Next.js
- TypeScript

后端：
- FastAPI
- Python

数据库：
- SQLite

AI 判题：
- LLM API（仅用于 SQL 手写题判题）

## 开发规则

1. 不要一次性开发整个系统。
2. 每次只完成一个明确的小功能。
3. 开发前先说明实施方案。
4. 不随意修改已经确定的核心架构。
5. 不为了优化而重构无关代码。
6. 新增功能时保持前后端接口一致。
7. 所有功能优先保证简单、可运行、可测试。
8. 如果存在不确定设计，先标记 TODO，不自行扩大需求。
9. 当前目标是秋招学习使用，不做过度设计。

## 开发流程

每个功能严格按照以下流程进行：

需求确认
→ 方案设计
→ 用户审核
→ 实施开发
→ 自动测试
→ 用户验收
→ Git 提交

未经用户审核，不得从方案设计直接进入实施开发。

## AI 评分规则

### SQL 手写题

- SQL 题不连接真实业务数据库执行。
- 由 LLM 根据题目、表结构、业务要求、参考答案和评分点进行辅助判题。
- AI 不只判断最终答案，还需要分析：
  - SQL 逻辑是否正确
  - JOIN 是否正确
  - 过滤条件是否完整
  - GROUP BY 是否正确
  - 窗口函数是否使用正确
  - 是否满足题目要求
- AI 评分结果需要给出原因和改进建议。

### 问答题

问答题 MVP 不调用 AI，不进行 AI 评分，不配置 scoring_rubric，不计算问答题分数。

流程：
1. 用户作答并提交。
2. 系统显示参考答案和解析。
3. 用户自己选择掌握状态：不会 / 模糊 / 基本掌握 / 熟练掌握。
4. 系统根据用户自评的掌握状态更新 ReviewState。

## 复习原则

平台采用基于遗忘规律的动态复习机制。

目标不是简单记录错题，而是根据用户的掌握情况安排下一次复习时间。

用户每次答题后需要形成掌握状态，例如：

- 不会
- 模糊
- 基本掌握
- 熟练掌握

不同掌握状态对应不同复习间隔。

具体算法在后续设计文档中确定，不在 CLAUDE.md 中写死实现细节。

## 代码变更审批规则

这是本项目的强制规则。

1. 对项目文件进行任何新建、修改、删除之前，必须先向用户说明实施方案。
2. 实施方案至少需要说明：
   - 为什么要修改
   - 准备修改哪些文件
   - 每个文件准备修改什么
   - 是否会影响现有功能、数据结构或接口
3. 未得到用户明确批准前，不得执行文件修改。
4. 用户批准后，只允许实施已批准范围内的修改。
5. 如果实施过程中发现需要扩大修改范围，必须停止并重新申请批准。
6. 不得因为“顺手优化”而修改与当前任务无关的代码。
7. 读取文件、分析代码、提出方案不需要审批；实际写入文件需要审批。

